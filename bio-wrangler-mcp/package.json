{
  "name": "bio-wrangler-mcp",
  "version": "0.1.0",
  "description": "MCP server for biomedical data wrangling with study.wrangler",
  "main": "dist/mcp-server.js",
  "bin": {
    "bio-wrangler-mcp": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "yarn build && node dist/mcp-server.js",
    "dev": "tsc --watch",
    "test": "jest"
  },
  "keywords": ["mcp", "biomedical", "data-wrangling", "r", "claude"],
  "author": "Study Wrangler Team",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "dockerode": "^4.0.2"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.29",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}